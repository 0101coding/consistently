# Frontend of Consistently

### **Welcome 👋**

To get started with this, clone this repo and follow these commands:

1. Run `npm install` at the root of your directory
2. Run `npm run start` to start the project after you cd into the frontent folder
3. Start coding!
